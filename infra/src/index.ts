// SST v3 infrastructure definitions will go here
// Placeholder until SST is configured

export const config = {
  app: "validatehome",
  region: "us-east-1",
  stages: ["dev", "staging", "production"],
} as const;
