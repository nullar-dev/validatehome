{
  "name": "@validatehome/rules-engine",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@validatehome/shared": "workspace:*",
    "json-rules-engine": "^6.5.0"
  },
  "devDependencies": {
    "@validatehome/config": "workspace:*",
    "vitest": "^4.0.18"
  }
}
